{
  "name": "action-setup-taskfile",
  "version": "1.0.0",
  "description": "GitHub Action to setup Taskfile (go-task) CLI",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist --source-map --license licenses.txt",
    "lint": "eslint src/**/*.ts",
    "test": "jest"
  },
  "keywords": [
    "github-action",
    "taskfile",
    "go-task",
    "task-runner",
    "automation"
  ],
  "author": "DiyRex",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/exec": "^1.1.1",
    "@actions/github": "^6.0.0",
    "@actions/http-client": "^2.2.1",
    "@actions/tool-cache": "^2.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.3.3"
  }
}
